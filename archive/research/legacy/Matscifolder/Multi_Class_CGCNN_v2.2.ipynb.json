{
 "cells": [
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "from google.colab import drive\n",
    "drive.mount('/content/drive')"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "import os, sys, shutil\n",
    "from __future__ import print_function, division\n",
    "import csv\n",
    "import functools\n",
    "import json\n",
    "import os\n",
    "import io\n",
    "import random\n",
    "import warnings\n",
    "import argparse\n",
    "import time\n",
    "import pprint\n",
    "import tempfile\n",
    "\n",
    "!pip install pymatgen > /dev/null 2>&1  # Suppress installation informational messages\n",
    "!pip install kmeans_pytorch > /dev/null 2>&1\n",
    "\n",
    "import zipfile\n",
    "from random import sample\n",
    "import numpy as np\n",
    "from sklearn import metrics\n",
    "import torch\n",
    "import torch.nn as nn\n",
    "from pymatgen.core.structure import Structure\n",
    "from pymatgen.io.cif import CifParser\n",
    "from torch.utils.data import Dataset, DataLoader\n",
    "from torch.utils.data.dataloader import default_collate\n",
    "from torch.utils.data.sampler import SubsetRandomSampler\n",
    "import torch.optim as optim\n",
    "from torch.autograd import Variable\n",
    "from torch.optim.lr_scheduler import MultiStepLR"
   ]
  },
  {
   "cell_type": "markdown",
   "source": [
    "Navigate to the folder where the following folders/files can be found;\n",
    "\n",
    "MOF data folder containing id_prop.csv file, atom.json file and a zipped folder containing cif files (NB: For the hMof database, this zipped file will also contain other json files. These json files contain the adsorption isoterm data that the model will be trained on)"
   ],
   "metadata": {}
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "cd /content/drive/MyDrive/MatSci2_Group/MultiClass_v2_group_folder"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "Run this block to initialize various classes for data processing\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "class AtomInitializer(object):\n",
    "    \"\"\"\n",
    "    Base class for intializing the vector representation for atoms.\n",
    "\n",
    "    !!! Use one AtomInitializer per dataset !!!\n",
    "    \"\"\"\n",
    "    def __init__(self, atom_types):\n",
    "        self.atom_types = set(atom_types)\n",
    "        self._embedding = {}\n",
    "\n",
    "    def get_atom_fea(self, atom_type):\n",
    "        assert atom_type in self.atom_types\n",
    "        return self._embedding[atom_type]\n",
    "\n",
    "    def load_state_dict(self, state_dict):\n",
    "        self._embedding = state_dict\n",
    "        self.atom_types = set(self._embedding.keys())\n",
    "        self._decodedict = {idx: atom_type for atom_type, idx in\n",
    "                            self._embedding.items()}\n",
    "\n",
    "\n",
    "    def state_dict(self):\n",
    "        return self._embedding\n",
    "\n",
    "    def decode(self, idx):\n",
    "        if not hasattr(self, '_decodedict'):\n",
    "            self._decodedict = {idx: atom_type for atom_type, idx in\n",
    "                                self._embedding.items()}\n",
    "        return self._decodedict[idx]\n",
    "\n",
    "class AtomCustomJSONInitializer(AtomInitializer):\n",
    "    \"\"\"\n",
    "    Initialize atom feature vectors using a JSON file, which is a python\n",
    "    dictionary mapping from element number to a list representing the\n",
    "    feature vector of the element.\n",
    "\n",
    "    Parameters\n",
    "    ----------\n",
    "\n",
    "    elem_embedding_file: str\n",
    "        The path to the .json file\n",
    "    \"\"\"\n",
    "    def __init__(self, elem_embedding_file): # The atom jason file is the element embedding file\n",
    "        with open(elem_embedding_file) as f:\n",
    "            elem_embedding = json.load(f)\n",
    "        elem_embedding = {int(key): value for key, value\n",
    "                          in elem_embedding.items()}\n",
    "        atom_types = set(elem_embedding.keys()) # Grabs all the keys of the element embeding without repetition\n",
    "\n",
    "        super(AtomCustomJSONInitializer, self).__init__(atom_types)\n",
    "        for key, value in elem_embedding.items():\n",
    "            self._embedding[key] = np.array(value, dtype=float) # Here it is capturing all the values of the atom jason dictionalry\n",
    "\n",
    "\n",
    "class GaussianDistance(object):\n",
    "    \"\"\"\n",
    "    Expands the distance by Gaussian basis.\n",
    "\n",
    "    Unit: angstrom\n",
    "    \"\"\"\n",
    "    def __init__(self, dmin, dmax, step, var=None):\n",
    "        \"\"\"\n",
    "        Parameters\n",
    "        ----------\n",
    "\n",
    "        dmin: float\n",
    "          Minimum interatomic distance\n",
    "        dmax: float\n",
    "          Maximum interatomic distance\n",
    "        step: float\n",
    "          Step size for the Gaussian filter\n",
    "        \"\"\"\n",
    "        assert dmin < dmax\n",
    "        assert dmax - dmin > step\n",
    "        self.filter = np.arange(dmin, dmax+step, step)\n",
    "        if var is None:\n",
    "            var = step\n",
    "        self.var = var\n",
    "\n",
    "    def expand(self, distances):\n",
    "        \"\"\"\n",
    "        Apply Gaussian disntance filter to a numpy distance array\n",
    "\n",
    "        Parameters\n",
    "        ----------\n",
    "\n",
    "        distance: np.array shape n-d array\n",
    "          A distance matrix of any shape\n",
    "\n",
    "        Returns\n",
    "        -------\n",
    "        expanded_distance: shape (n+1)-d array\n",
    "          Expanded distance matrix with the last dimension of length\n",
    "          len(self.filter)\n",
    "        \"\"\"\n",
    "        return np.exp(-(distances[..., np.newaxis] - self.filter)**2 /\n",
    "                      self.var**2)\n",
    "\n",
    "# Modified by Kwabena Asiedu Date: 14th January 2023\n",
    "class CIFData(Dataset):\n",
    "    def __init__(self, root_dir,root_zip, max_num_nbr=12, radius=8, dmin=0, step=0.2,\n",
    "                 random_seed=123):\n",
    "        self.root_dir = root_dir\n",
    "        self.root_zip = root_zip\n",
    "        self.max_num_nbr, self.radius = max_num_nbr, radius\n",
    "        assert os.path.exists(root_dir), 'root_dir does not exist!'\n",
    "        id_prop_file = os.path.join(self.root_dir, 'id_prop.csv')\n",
    "        assert os.path.exists(id_prop_file), 'id_prop.csv does not exist!'\n",
    "        with open(id_prop_file) as f:\n",
    "            reader = csv.reader(f)\n",
    "            self.id_prop_data = [row for row in reader]\n",
    "            self.feat_vect = {sublist[0]:sublist[1:] for sublist in self.id_prop_data}  # Extracts all the strings from the csv file\n",
    "            self.feat_vect = {k:list(map(float, v)) for k,v in self.feat_vect.items()}  # converts all the string integers into floats\n",
    "        random.seed(random_seed)\n",
    "        random.shuffle(self.id_prop_data)\n",
    "        atom_init_file = os.path.join(self.root_dir, 'atom_init.json')\n",
    "        assert os.path.exists(atom_init_file), 'atom_init.json does not exist!'\n",
    "        self.ari = AtomCustomJSONInitializer(atom_init_file)\n",
    "        self.gdf = GaussianDistance(dmin=dmin, dmax=self.radius, step=step)\n",
    "\n",
    "    def __len__(self):\n",
    "        return len(self.id_prop_data)\n",
    "\n",
    "    @functools.lru_cache(maxsize=None)  # Cache loaded structures\n",
    "    # Later: Allow for vector calling\n",
    "    def __getitem__(self, idx):\n",
    "        keys, values = zip(*self.feat_vect.items()) # Extracts the dictionary into two tuples (tuple unpacking)\n",
    "        cif_id, target = keys[idx], values[idx]\n",
    "        # Open the zip file and get the CIF file object\n",
    "        with zipfile.ZipFile(os.path.join(self.root_dir, self.root_zip+'.zip'), \"r\") as z:\n",
    "           with tempfile.TemporaryDirectory() as tmpdir:\n",
    "              # Extract the CIF file to the temporary directory\n",
    "              z.extract(cif_id+'.cif', path=tmpdir)\n",
    "              # Create a Structure object from the CIF file\n",
    "              crystal = Structure.from_file(os.path.join(tmpdir, cif_id+'.cif'))\n",
    "\n",
    "        #crystal = Structure.from_file(os.path.join(self.root_dir,\n",
    "        #                                          cif_id+'.cif'))\n",
    "        atom_fea = np.vstack([self.ari.get_atom_fea(crystal[i].specie.number)\n",
    "                              for i in range(len(crystal))])\n",
    "        atom_fea = torch.Tensor(atom_fea)\n",
    "        all_nbrs = crystal.get_all_neighbors(self.radius, include_index=True)\n",
    "        all_nbrs = [sorted(nbrs, key=lambda x: x[1]) for nbrs in all_nbrs]\n",
    "        nbr_fea_idx, nbr_fea = [], []\n",
    "        for nbr in all_nbrs:\n",
    "            if len(nbr) < self.max_num_nbr:\n",
    "                warnings.warn('{} not find enough neighbors to build graph. '\n",
    "                              'If it happens frequently, consider increase '\n",
    "                              'radius.'.format(cif_id))\n",
    "                nbr_fea_idx.append(list(map(lambda x: x[2], nbr)) +\n",
    "                                   [0] * (self.max_num_nbr - len(nbr)))\n",
    "                nbr_fea.append(list(map(lambda x: x[1], nbr)) +\n",
    "                               [self.radius + 1.] * (self.max_num_nbr -\n",
    "                                                     len(nbr)))\n",
    "            else:\n",
    "                nbr_fea_idx.append(list(map(lambda x: x[2],\n",
    "                                            nbr[:self.max_num_nbr])))\n",
    "                nbr_fea.append(list(map(lambda x: x[1],\n",
    "                                        nbr[:self.max_num_nbr])))\n",
    "        nbr_fea_idx, nbr_fea = np.array(nbr_fea_idx), np.array(nbr_fea)\n",
    "        nbr_fea = self.gdf.expand(nbr_fea)\n",
    "        atom_fea = torch.Tensor(atom_fea)\n",
    "\n",
    "        nbr_fea = torch.Tensor(nbr_fea)\n",
    "        nbr_fea_idx = torch.LongTensor(nbr_fea_idx)\n",
    "        target = torch.Tensor(target)\n",
    "        return (atom_fea, nbr_fea, nbr_fea_idx), target, cif_id\n",
    "\n",
    "\n",
    "\n",
    "def collate_pool(dataset_list):\n",
    "\n",
    "    \"\"\"\n",
    "    Collate a list of data and return a batch for predicting crystal\n",
    "    properties.\n",
    "\n",
    "    Parameters\n",
    "    ----------\n",
    "\n",
    "    dataset_list: list of tuples for each data point.\n",
    "      (atom_fea, nbr_fea, nbr_fea_idx, target) # The dataset_list is defined here\n",
    "\n",
    "      atom_fea: torch.Tensor shape (n_i, atom_fea_len)\n",
    "      nbr_fea: torch.Tensor shape (n_i, M, nbr_fea_len)\n",
    "      nbr_fea_idx: torch.LongTensor shape (n_i, M)\n",
    "      target: torch.Tensor shape (1, )\n",
    "      cif_id: str or int\n",
    "\n",
    "    Returns\n",
    "    -------\n",
    "    N = sum(n_i); N0 = sum(i)\n",
    "\n",
    "    batch_atom_fea: torch.Tensor shape (N, orig_atom_fea_len)\n",
    "      Atom features from atom type\n",
    "    batch_nbr_fea: torch.Tensor shape (N, M, nbr_fea_len)\n",
    "      Bond features of each atom's M neighbors\n",
    "    batch_nbr_fea_idx: torch.LongTensor shape (N, M)\n",
    "      Indices of M neighbors of each atom\n",
    "    crystal_atom_idx: list of torch.LongTensor of length N0\n",
    "      Mapping from the crystal idx to atom idx\n",
    "    target: torch.Tensor shape (N, 1)\n",
    "      Target value for prediction\n",
    "    batch_cif_ids: list\n",
    "    \"\"\"\n",
    "\n",
    "    batch_atom_fea, batch_nbr_fea, batch_nbr_fea_idx = [], [], []\n",
    "    crystal_atom_idx, batch_target = [], []\n",
    "    batch_cif_ids = []\n",
    "    base_idx = 0\n",
    "    for i, ((atom_fea, nbr_fea, nbr_fea_idx), target, cif_id)\\\n",
    "            in enumerate(dataset_list):\n",
    "        n_i = atom_fea.shape[0]  # number of atoms for this crystal\n",
    "        batch_atom_fea.append(atom_fea)\n",
    "        batch_nbr_fea.append(nbr_fea)\n",
    "        batch_nbr_fea_idx.append(nbr_fea_idx+base_idx)\n",
    "        new_idx = torch.LongTensor(np.arange(n_i)+base_idx)\n",
    "        crystal_atom_idx.append(new_idx)\n",
    "        batch_target.append(target)\n",
    "        batch_cif_ids.append(cif_id)\n",
    "        base_idx += n_i\n",
    "\n",
    "    return (torch.cat(batch_atom_fea, dim=0),\n",
    "            torch.cat(batch_nbr_fea, dim=0),\n",
    "            torch.cat(batch_nbr_fea_idx, dim=0),\n",
    "            crystal_atom_idx),\\\n",
    "        torch.stack(batch_target, dim=0),\\\n",
    "        batch_cif_ids\n",
    "\n",
    "\n",
    "def get_train_val_test_loader(dataset, collate_fn=collate_pool,\n",
    "                              batch_size=64, train_ratio=None,\n",
    "                              val_ratio=0.1, test_ratio=0.1, return_test=False,\n",
    "                              num_workers=1, pin_memory=False, **kwargs):\n",
    "    \"\"\"\n",
    "    Utility function for dividing a dataset to train, val, test datasets.\n",
    "\n",
    "    !!! The dataset needs to be shuffled before using the function !!!\n",
    "\n",
    "    Parameters\n",
    "    ----------\n",
    "    dataset: torch.utils.data.Dataset\n",
    "      The full dataset to be divided.\n",
    "    collate_fn: torch.utils.data.DataLoader\n",
    "    batch_size: int\n",
    "    train_ratio: float\n",
    "    val_ratio: float\n",
    "    test_ratio: float\n",
    "    return_test: bool\n",
    "      Whether to return the test dataset loader. If False, the last test_size\n",
    "      data will be hidden.\n",
    "    num_workers: int\n",
    "    pin_memory: bool\n",
    "\n",
    "    Returns\n",
    "    -------\n",
    "    train_loader: torch.utils.data.DataLoader\n",
    "      DataLoader that random samples the training data.\n",
    "    val_loader: torch.utils.data.DataLoader\n",
    "      DataLoader that random samples the validation data.\n",
    "    (test_loader): torch.utils.data.DataLoader\n",
    "      DataLoader that random samples the test data, returns if\n",
    "        return_test=True.\n",
    "    \"\"\"\n",
    "    total_size = len(dataset)\n",
    "    if kwargs['train_size'] is None:\n",
    "        if train_ratio is None:\n",
    "            assert val_ratio + test_ratio < 1\n",
    "            train_ratio = 1 - val_ratio - test_ratio\n",
    "            print(f'[Warning] train_ratio is None, using 1 - val_ratio - '\n",
    "                  f'test_ratio = {train_ratio} as training data.')\n",
    "        else:\n",
    "            assert train_ratio + val_ratio + test_ratio <= 1\n",
    "    indices = list(range(total_size))\n",
    "    if kwargs['train_size']:\n",
    "        train_size = kwargs['train_size']\n",
    "    else:\n",
    "        train_size = int(train_ratio * total_size)\n",
    "    if kwargs['test_size']:\n",
    "        test_size = kwargs['test_size']\n",
    "    else:\n",
    "        test_size = int(test_ratio * total_size)\n",
    "    if kwargs['val_size']:\n",
    "        valid_size = kwargs['val_size']\n",
    "    else:\n",
    "        valid_size = int(val_ratio * total_size)\n",
    "    train_sampler = SubsetRandomSampler(indices[:train_size])\n",
    "    val_sampler = SubsetRandomSampler(\n",
    "        indices[-(valid_size + test_size):-test_size])\n",
    "    if return_test:\n",
    "        test_sampler = SubsetRandomSampler(indices[-test_size:])\n",
    "    train_loader = DataLoader(dataset, batch_size=batch_size,\n",
    "                              sampler=train_sampler,\n",
    "                              num_workers=num_workers,\n",
    "                              collate_fn=collate_fn, pin_memory=pin_memory)\n",
    "    val_loader = DataLoader(dataset, batch_size=batch_size,\n",
    "                            sampler=val_sampler,\n",
    "                            num_workers=num_workers,\n",
    "                            collate_fn=collate_fn, pin_memory=pin_memory)\n",
    "    if return_test:\n",
    "        test_loader = DataLoader(dataset, batch_size=batch_size,\n",
    "                                 sampler=test_sampler,\n",
    "                                 num_workers=num_workers,\n",
    "                                 collate_fn=collate_pool, pin_memory=pin_memory)\n",
    "    if return_test:\n",
    "        return train_loader, val_loader, test_loader\n",
    "    else:\n",
    "        return train_loader, val_loader"
   ]
  },
  {
   "cell_type": "markdown",
   "source": [
    "For the CIFData object input parameters, simply enter the names of the names of the folders containing the  id_prop.csv file and atom.json for the first parameter eg: hMOF Database. For the second parameter enter the name of the zipped folder eg:hMOF 10 1039 C2EE23201D all."
   ],
   "metadata": {}
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Obtaining input data\n",
    "testing = CIFData('hMOF Database','hMOF 10 1039 C2EE23201D all')\n",
    "dataset_list = testing\n",
    "\n",
    "if len(dataset_list) < 500:\n",
    "            warnings.warn('Dataset has less than 500 data points. '\n",
    "                          'Lower accuracy is expected. ')"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Recording the initial atom features\n",
    "\n",
    "structures, _, _ = testing[0] # Tuple unpacking to access the number of columns in the original atom features\n",
    "orig_atom_fea_len = structures[0].shape[-1]\n",
    "nbr_fea_len = structures[1].shape[-1]\n"
   ]
  },
  {
   "cell_type": "markdown",
   "source": [
    "Specify the number of class labels based on your prepared id_prop.csv file"
   ],
   "metadata": {}
  },
  {
   "cell_type": "code",
   "source": [
    "# specify the number of class labels\n",
    "classnum = 4\n"
   ],
   "metadata": {},
   "execution_count": null,
   "outputs": []
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "**MODEL DESCRIPTION:**\n",
    "The model performs a convolution operation on a crystal graph and additional MOF geometric features and then outputs a tensor representing probability distributuions of potential class labels."
   ]
  },
  {
   "cell_type": "code",
   "source": [
    " # create a device object to move data from CPU to GPU\n",
    "deviceGpu = torch.device('cuda:0') # create a device object\n",
    "deviceCpu = device = torch.device('cpu') # create a device object"
   ],
   "metadata": {},
   "execution_count": null,
   "outputs": []
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "class ConvLayer(nn.Module):  # we define the CGCNN here so that it can be later called and used in the foward pass\n",
    "    \"\"\"\n",
    "    Convolutional operation on graphs\n",
    "    \"\"\"\n",
    "    def __init__(self, atom_fea_len, nbr_fea_len):\n",
    "        \"\"\"\n",
    "        Initialize ConvLayer.\n",
    "\n",
    "        Parameters\n",
    "        ----------\n",
    "\n",
    "        atom_fea_len: int\n",
    "          Number of atom hidden features.\n",
    "        nbr_fea_len: int\n",
    "          Number of bond features.\n",
    "        \"\"\"\n",
    "        super(ConvLayer, self).__init__()\n",
    "        self.atom_fea_len = atom_fea_len\n",
    "        self.nbr_fea_len = nbr_fea_len\n",
    "        self.fc_full = nn.Linear(2*self.atom_fea_len+self.nbr_fea_len,\n",
    "                                 2*self.atom_fea_len)\n",
    "        self.sigmoid = nn.Sigmoid()\n",
    "        self.softplus1 = nn.Softplus()\n",
    "        self.bn1 = nn.BatchNorm1d(2*self.atom_fea_len)\n",
    "        self.bn2 = nn.BatchNorm1d(self.atom_fea_len)\n",
    "        self.softplus2 = nn.Softplus()\n",
    "\n",
    "    def forward(self, atom_in_fea, nbr_fea, nbr_fea_idx):\n",
    "        \"\"\"\n",
    "        Forward pass\n",
    "\n",
    "        N: Total number of atoms in the batch\n",
    "        M: Max number of neighbors\n",
    "\n",
    "        Parameters\n",
    "        ----------\n",
    "\n",
    "        atom_in_fea: Variable(torch.Tensor) shape (N, atom_fea_len)\n",
    "          Atom hidden features before convolution\n",
    "        nbr_fea: Variable(torch.Tensor) shape (N, M, nbr_fea_len)\n",
    "          Bond features of each atom's M neighbors\n",
    "        nbr_fea_idx: torch.LongTensor shape (N, M)\n",
    "          Indices of M neighbors of each atom\n",
    "\n",
    "        Returns\n",
    "        -------\n",
    "\n",
    "        atom_out_fea: nn.Variable shape (N, atom_fea_len)\n",
    "          Atom hidden features after convolution\n",
    "\n",
    "        \"\"\"\n",
    "        # TODO will there be problems with the index zero padding?\n",
    "        N, M = nbr_fea_idx.shape\n",
    "        # convolution\n",
    "        atom_nbr_fea = atom_in_fea[nbr_fea_idx, :]\n",
    "        total_nbr_fea = torch.cat(\n",
    "            [atom_in_fea.unsqueeze(1).expand(N, M, self.atom_fea_len),\n",
    "             atom_nbr_fea, nbr_fea], dim=2)\n",
    "        total_gated_fea = self.fc_full(total_nbr_fea)\n",
    "        total_gated_fea = self.bn1(total_gated_fea.view(\n",
    "            -1, self.atom_fea_len*2)).view(N, M, self.atom_fea_len*2)\n",
    "        nbr_filter, nbr_core = total_gated_fea.chunk(2, dim=2)\n",
    "        nbr_filter = self.sigmoid(nbr_filter)\n",
    "        nbr_core = self.softplus1(nbr_core)\n",
    "        nbr_sumed = torch.sum(nbr_filter * nbr_core, dim=1)\n",
    "        nbr_sumed = self.bn2(nbr_sumed)\n",
    "        out = self.softplus2(atom_in_fea + nbr_sumed)\n",
    "        return out\n",
    "\n",
    "\n",
    "class CrystalGraphConvNet_1(nn.Module): # The main calls this function only once because it will by itself call the ConvLayer\n",
    "    \"\"\"\n",
    "    Create a crystal graph convolutional neural network for predicting total\n",
    "    material properties.\n",
    "    \"\"\"\n",
    "    def __init__(self, orig_atom_fea_len, nbr_fea_len,added_fea_len,\n",
    "                 atom_fea_len=64, n_conv=4, h_fea_len=128, n_h=4,\n",
    "                 classification=True): # This is where the convolutional layers and hidden layers can be adjusted\n",
    "        \"\"\"\n",
    "        Initialize CrystalGraphConvNet.\n",
    "\n",
    "        Parameters\n",
    "        ----------\n",
    "\n",
    "        orig_atom_fea_len: int\n",
    "          Number of atom features in the input.\n",
    "        nbr_fea_len: int\n",
    "          Number of bond features.\n",
    "        atom_fea_len: int\n",
    "          Number of hidden atom features in the convolutional layers\n",
    "        n_conv: int\n",
    "          Number of convolutional layers\n",
    "        h_fea_len: int\n",
    "          Number of hidden features after pooling\n",
    "        n_h: int\n",
    "          Number of hidden layers after pooling\n",
    "        \"\"\"\n",
    "        super(CrystalGraphConvNet_1, self).__init__()\n",
    "        self.classification = classification\n",
    "        self.added_fea_len = added_fea_len\n",
    "        self.embedding = nn.Linear(orig_atom_fea_len, atom_fea_len)\n",
    "        self.convs = nn.ModuleList([ConvLayer(atom_fea_len=atom_fea_len,\n",
    "                                    nbr_fea_len=nbr_fea_len)\n",
    "                                    for _ in range(n_conv)])  # This is how more convolutional layers are added\n",
    "        #self.conv_to_fc = nn.Linear(atom_fea_len, h_fea_len)\n",
    "        self.conv_to_fc = nn.Linear(atom_fea_len+self.added_fea_len, h_fea_len)\n",
    "        self.conv_to_fc_softplus = nn.Softplus()\n",
    "        if n_h > 1:  # This is how more hidden layers are added\n",
    "            self.fcs = nn.ModuleList([nn.Linear(h_fea_len, h_fea_len)\n",
    "                                      for _ in range(n_h-1)])\n",
    "            self.softpluses = nn.ModuleList([nn.Softplus()\n",
    "                                             for _ in range(n_h-1)])\n",
    "        if self.classification:\n",
    "            self.fc_out = nn.Linear(h_fea_len, classnum) # This is where the number of classes can be changed\n",
    "\n",
    "        self.softmax = nn.Softmax(dim=1)  # Softmax for multiclass classification\n",
    "        self.logsoftmax = nn.LogSoftmax(dim=1) # For negative log-likelihood loss\n",
    "        self.dropout = nn.Dropout()\n",
    "\n",
    "    def getFeatures_Targets(self,targets,**kwargs):\n",
    "      # Obtain additional features from the target vector\n",
    "      self.targets = targets\n",
    "      self.test = kwargs['test']\n",
    "      # Check to see if the model is being trained or being used for a prediction\n",
    "      if kwargs['test']!=False:\n",
    "        self.features = self.targets\n",
    "      else:\n",
    "        self.features = self.targets[:, :-1]\n",
    "        self.label = self.targets[:,-1]\n",
    "\n",
    "      # A variable to specify the number of additional nodes during training\n",
    "      #self.added_fea_len = self.features.shape[1]\n",
    "\n",
    "      return self.features, self.label.view(-1).long() if self.test==False else None, self.added_fea_len, self.test\n",
    "\n",
    "    def forward(self, atom_fea, nbr_fea, nbr_fea_idx, crystal_atom_idx,atom_fea_len=64,h_fea_len=128):  # This applies your defined neural network to your input data\n",
    "        \"\"\"\n",
    "        Forward pass\n",
    "\n",
    "        N: Total number of atoms in the batch\n",
    "        M: Max number of neighbors\n",
    "        N0: Total number of crystals in the batch\n",
    "\n",
    "        Parameters\n",
    "        ----------\n",
    "\n",
    "        atom_fea: Variable(torch.Tensor) shape (N, orig_atom_fea_len)\n",
    "          Atom features from atom type\n",
    "        nbr_fea: Variable(torch.Tensor) shape (N, M, nbr_fea_len)\n",
    "          Bond features of each atom's M neighbors\n",
    "        nbr_fea_idx: torch.LongTensor shape (N, M)\n",
    "          Indices of M neighbors of each atom\n",
    "        crystal_atom_idx: list of torch.LongTensor of length N0\n",
    "          Mapping from the crystal idx to atom idx\n",
    "\n",
    "        Returns\n",
    "        -------\n",
    "\n",
    "        prediction: nn.Variable shape (N, )\n",
    "          Atom hidden features after convolution\n",
    "\n",
    "        \"\"\"\n",
    "\n",
    "        atom_fea = self.embedding(atom_fea)\n",
    "        for conv_func in self.convs:\n",
    "            atom_fea = conv_func(atom_fea, nbr_fea, nbr_fea_idx)\n",
    "        crys_fea = self.pooling(atom_fea, crystal_atom_idx)\n",
    "        normalizer = Normalizer(self.features)\n",
    "        MoFprops = normalizer.norm(self.features)\n",
    "        crys_fea = torch.cat((crys_fea, MoFprops), dim=1)\n",
    "        #print('Poolcrys_fea:',crys_fea)\n",
    "        #self.conv_to_fc2 = nn.Linear(atom_fea_len + self.added_fea_len, h_fea_len)\n",
    "        crys_fea = self.conv_to_fc(self.conv_to_fc_softplus(crys_fea))\n",
    "        crys_fea = self.conv_to_fc_softplus(crys_fea)\n",
    "        #Crys_feat = crys_fea\n",
    "        #print('Crys_feat:',crys_fea,'\\n\\n')\n",
    "        if self.classification:\n",
    "            crys_fea = self.dropout(crys_fea)\n",
    "        if hasattr(self, 'fcs') and hasattr(self, 'softpluses'):\n",
    "            for fc, softplus in zip(self.fcs, self.softpluses):\n",
    "                crys_fea = softplus(fc(crys_fea))\n",
    "        out = self.fc_out(crys_fea)\n",
    "        out = self.logsoftmax(out)\n",
    "        #out = self.softmax(out)  # Apply softmax activation for multiclass classification\n",
    "\n",
    "\n",
    "        return out, self.label if not self.test else None\n",
    "\n",
    "\n",
    "    def pooling(self, atom_fea, crystal_atom_idx):\n",
    "        \"\"\"\n",
    "        Pooling the atom features to crystal features\n",
    "\n",
    "        N: Total number of atoms in the batch\n",
    "        N0: Total number of crystals in the batch\n",
    "\n",
    "        Parameters\n",
    "        ----------\n",
    "\n",
    "        atom_fea: Variable(torch.Tensor) shape (N, atom_fea_len)\n",
    "          Atom feature vectors of the batch\n",
    "        crystal_atom_idx: list of torch.LongTensor of length N0\n",
    "          Mapping from the crystal idx to atom idx\n",
    "        \"\"\"\n",
    "        assert sum([len(idx_map) for idx_map in crystal_atom_idx]) ==\\\n",
    "            atom_fea.data.shape[0]\n",
    "        summed_fea = [torch.mean(atom_fea[idx_map], dim=0, keepdim=True)\n",
    "                      for idx_map in crystal_atom_idx]\n",
    "        return torch.cat(summed_fea, dim=0)\n",
    "\n",
    "\n",
    "\n",
    "class Normalizer(object):\n",
    "    \"\"\"Normalize a Tensor column by column and restore it later. \"\"\"\n",
    "\n",
    "    def __init__(self, tensor):\n",
    "        \"\"\"tensor is taken as a sample to calculate the mean and std\"\"\"\n",
    "        self.mean = torch.mean(tensor, dim=0)\n",
    "        self.std = torch.std(tensor, dim=0)\n",
    "\n",
    "    def norm(self, tensor):\n",
    "        return (tensor - self.mean) / self.std\n",
    "\n",
    "    def denorm(self, normed_tensor):\n",
    "        return normed_tensor * self.std + self.mean\n",
    "\n",
    "    def state_dict(self):\n",
    "        return {'mean': self.mean,\n",
    "                'std': self.std}\n",
    "\n",
    "    def load_state_dict(self, state_dict):\n",
    "        self.mean = state_dict['mean']\n",
    "        self.std = state_dict['std']\n",
    "\n",
    "\n",
    "class AverageMeter(object):\n",
    "    \"\"\"Computes and stores the average and current value\"\"\"\n",
    "\n",
    "    def __init__(self):\n",
    "        self.reset()\n",
    "\n",
    "    def reset(self):\n",
    "        self.val = 0\n",
    "        self.avg = 0\n",
    "        self.sum = 0\n",
    "        self.count = 0\n",
    "\n",
    "    def update(self, val, n=1):\n",
    "        self.val = val\n",
    "        self.sum += val * n\n",
    "        self.count += n\n",
    "        self.avg = self.sum / self.count\n",
    "\n",
    "    def numpy(self):\n",
    "        self.avg = self.avg.numpy()"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Initiate a model and adjust hyper parameters\n",
    "TheModel_1 = CrystalGraphConvNet_1(orig_atom_fea_len,nbr_fea_len,\n",
    "                                   added_fea_len = 5,\n",
    "                                   atom_fea_len=64,\n",
    "                                   n_conv=4,\n",
    "                                   h_fea_len=128,\n",
    "                                   n_h=4,\n",
    "                                  classification=True)\n",
    "# It is best if you take critical note of your hyper parameters\n",
    "\n",
    "#TheModel_1 = TheModel_1.to(device)"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Splittig data into training, testing, and validation\n",
    "\n",
    "# batch_size by convention is set to be 32\n",
    "collate_fn = collate_pool\n",
    "train_loader, val_loader, test_loader = get_train_val_test_loader(\n",
    "        dataset= testing,\n",
    "        collate_fn=collate_fn,\n",
    "        batch_size=64, # This controlls the nmber of samples per training iter in the train def function, respectively for training validation and testing\n",
    "        train_ratio=0.6,\n",
    "        num_workers=1,\n",
    "        val_ratio=0.1,\n",
    "        test_ratio=0.3,\n",
    "        pin_memory=True, # Default was True\n",
    "        train_size=None,\n",
    "        val_size=None,\n",
    "        test_size=None,\n",
    "        return_test=True)\n"
   ]
  },
  {
   "cell_type": "code",
   "source": [
    "# Define Optimizer\n",
    "optimizer = optim.Adam(TheModel_1.parameters(), lr= 0.01,\n",
    "                            weight_decay= 0) # Adjust learning rates\n",
    "# Defining loss\n",
    "criterion = nn.NLLLoss()\n",
    "\n",
    "# Determines other performance metricies\n",
    "def class_eval(prediction, target):\n",
    "    prediction = np.exp(prediction.numpy())\n",
    "    target = target.cpu().numpy()\n",
    "    pred_label = np.argmax(prediction, axis=1)\n",
    "    target_label = np.squeeze(target)\n",
    "    if not target_label.shape:\n",
    "        target_label = np.asarray([target_label])\n",
    "    precision, recall, fscore, _ = metrics.precision_recall_fscore_support(\n",
    "        target_label, pred_label, average='weighted')\n",
    "    accuracy = metrics.accuracy_score(target_label, pred_label)\n",
    "    return accuracy, precision, recall, fscore\n"
   ],
   "metadata": {},
   "execution_count": null,
   "outputs": []
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "def train(train_loader, model, optimizer, epoch):\n",
    "\n",
    "    # Keeping track of performance and recording training averages\n",
    "    batch_time = AverageMeter()\n",
    "    data_time = AverageMeter()\n",
    "    losses  = AverageMeter()\n",
    "\n",
    "    accuracies = AverageMeter()\n",
    "    precisions = AverageMeter()\n",
    "    recalls = AverageMeter()\n",
    "    fscores = AverageMeter()\n",
    "\n",
    "\n",
    "    # switch to train mode\n",
    "    model.train()\n",
    "    model.to(deviceGpu)\n",
    "\n",
    "\n",
    "    end = time.time()\n",
    "    for i, (input, target,*args) in enumerate(train_loader):\n",
    "        # measure data loading time\n",
    "        data_time.update(time.time() - end)\n",
    "\n",
    "        input_var = (Variable(input[0].cuda(non_blocking=True)),\n",
    "                         Variable(input[1].cuda(non_blocking=True)),\n",
    "                         input[2].cuda(non_blocking=True),\n",
    "                         [crys_idx.cuda(non_blocking=True) for crys_idx in input[3]])\n",
    "\n",
    "\n",
    "        # Obtain Model Targets\n",
    "        model.getFeatures_Targets(target.to(deviceGpu),test=False)\n",
    "\n",
    "         # compute output  and load data back to cpu memory\n",
    "        #output,target_label = model(input_var[0].to(deviceCpu),\n",
    "         #                           input_var[1].to(deviceCpu),\n",
    "          #                          input_var[2].to(deviceCpu),\n",
    "           #                         [crys_idx.to(deviceCpu) for crys_idx in input_var[3]])\n",
    "        output,target_label = model(*input_var)\n",
    "\n",
    "        target_var = Variable(target_label.long())\n",
    "\n",
    "\n",
    "        loss = criterion(output,target_var)\n",
    "\n",
    "        # measure model accuracy performance and record averages for each\n",
    "        accuracy, precision, recall, fscore = \\\n",
    "                class_eval(output.data.cpu(), target_var)\n",
    "\n",
    "        # Record and average metricies\n",
    "        losses.update(loss.data.cpu().item(), target_var.size(0))\n",
    "        accuracies.update(accuracy, target_var.size(0))\n",
    "        precisions.update(precision, target_var.size(0))\n",
    "        recalls.update(recall, target_var.size(0))\n",
    "        fscores.update(fscore, target_var.size(0))\n",
    "\n",
    "\n",
    "        # compute gradient and do SGD step\n",
    "        optimizer.zero_grad()\n",
    "        loss.backward()\n",
    "        optimizer.step()\n",
    "\n",
    "        # measure elapsed times\n",
    "        batch_time.update(time.time() - end)\n",
    "        end = time.time()\n",
    "\n",
    "\n",
    "        print('Epoch: [{0}][{1}/{2}]\\t'\n",
    "                      'Time {batch_time.val:.3f} ({batch_time.avg:.3f})\\t'\n",
    "                      'Data {data_time.val:.3f} ({data_time.avg:.3f})\\t'\n",
    "                      'Loss {loss.val:.4f} ({loss.avg:.4f})\\t'\n",
    "                      'Accu {accu.val:.3f} ({accu.avg:.3f})\\t'\n",
    "                      'Precision {prec.val:.3f} ({prec.avg:.3f})\\t'\n",
    "                      'Recall {recall.val:.3f} ({recall.avg:.3f})\\t'\n",
    "                      'F1 {f1.val:.3f} ({f1.avg:.3f})'.format(\n",
    "                    epoch, i, len(train_loader), batch_time=batch_time,\n",
    "                    data_time=data_time, loss=losses, accu=accuracies,\n",
    "                    prec=precisions, recall=recalls, f1=fscores)\n",
    "                )\n",
    "\n",
    "def validate(val_loader, model, criterion, test=False):\n",
    "    batch_time = AverageMeter()\n",
    "    losses = AverageMeter()\n",
    "    accuracies = AverageMeter()\n",
    "    precisions = AverageMeter()\n",
    "    recalls = AverageMeter()\n",
    "    fscores = AverageMeter()\n",
    "    if test:\n",
    "      test_targets = []\n",
    "      test_preds = []\n",
    "      test_cif_ids = []\n",
    "\n",
    "    # switch to evaluate mode\n",
    "    model.eval()\n",
    "\n",
    "    end = time.time()\n",
    "\n",
    "    for i, (input, target, batch_cif_ids) in enumerate(val_loader):\n",
    "      with torch.no_grad():\n",
    "                input_var = (Variable(input[0].cuda(non_blocking=True)),\n",
    "                         Variable(input[1].cuda(non_blocking=True)),\n",
    "                         input[2].cuda(non_blocking=True),\n",
    "                         [crys_idx.cuda(non_blocking=True) for crys_idx in input[3]])\n",
    "\n",
    "      # Obtain Model Targets\n",
    "      model.getFeatures_Targets(target,test=False)\n",
    "\n",
    "      # compute output\n",
    "      output,target_label = model(input_var[0].to(deviceCpu),\n",
    "                                    input_var[1].to(deviceCpu),\n",
    "                                    input_var[2].to(deviceCpu),\n",
    "                                    [crys_idx.to(deviceCpu) for crys_idx in input_var[3]])\n",
    "      #output,target_label = model(*input_var)\n",
    "\n",
    "\n",
    "\n",
    "      target_var = Variable(target_label.long())\n",
    "\n",
    "      loss = criterion(output, target_var)\n",
    "\n",
    "      # Compute the metrices\n",
    "      accuracy, precision, recall, fscore = \\\n",
    "              class_eval(output.cpu(), target_var)\n",
    "\n",
    "      losses.update(loss.data.cpu().item(), target_var.size(0))\n",
    "      accuracies.update(accuracy, target_var.size(0))\n",
    "      precisions.update(precision, target_var.size(0))\n",
    "      recalls.update(recall,target_var.size(0))\n",
    "      fscores.update(fscore, target_var.size(0))\n",
    "\n",
    "      # Gather the model predictions\n",
    "      if test:\n",
    "            test_pred = torch.exp(output.data.cpu())\n",
    "\n",
    "            test_target = target_var\n",
    "            assert test_pred.shape[1] == classnum\n",
    "            test_pred = torch.argmax(test_pred,axis=1)\n",
    "            test_preds += test_pred[:].tolist()\n",
    "            test_targets += test_target.view(-1).tolist()\n",
    "            test_cif_ids += batch_cif_ids\n",
    "\n",
    "      # measure elapsed time\n",
    "      batch_time.update(time.time() - end)\n",
    "      end = time.time()\n",
    "\n",
    "\n",
    "\n",
    "      print('Test:[{0}/{1}]\\t'\n",
    "                      'Time {batch_time.val:.3f} ({batch_time.avg:.3f})\\t'\n",
    "                      'Loss {loss.val:.4f} ({loss.avg:.4f})\\t'\n",
    "                      'Accu {accu.val:.3f} ({accu.avg:.3f})\\t'\n",
    "                      'Precision {prec.val:.3f} ({prec.avg:.3f})\\t'\n",
    "                      'Recall {recall.val:.3f} ({recall.avg:.3f})\\t'\n",
    "                      'F1 {f1.val:.3f} ({f1.avg:.3f})\\t'.format(\n",
    "                    i, len(val_loader), batch_time=batch_time, loss=losses,\n",
    "                    accu=accuracies, prec=precisions, recall=recalls,\n",
    "                    f1=fscores)\n",
    "              )\n",
    "\n",
    "\n",
    "      if test:\n",
    "        star_label = '**'\n",
    "        import csv\n",
    "        with open('test_results.csv', 'w') as f:\n",
    "            writer = csv.writer(f)\n",
    "            for cif_id, target, pred in zip(test_cif_ids, test_targets,\n",
    "                                            test_preds):\n",
    "                writer.writerow((cif_id, target, pred))\n",
    "\n",
    "        print(' {star} AUC {accu.avg:.3f}'.format(star=star_label,\n",
    "                                                accu=accuracies))\n",
    "\n",
    "    return accuracies.avg\n",
    "\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "scheduler = MultiStepLR(optimizer, milestones=[100],gamma=0.1)\n",
    "\n",
    "def save_checkpoint(state, is_best, filename='checkpoint.pth.tar'):\n",
    "    torch.save(state, filename)\n",
    "    if is_best:\n",
    "      shutil.copyfile(filename, 'model_best.pth.tar')\n"
   ]
  },
  {
   "cell_type": "markdown",
   "source": [
    "Resume training from a checkpoint if specified"
   ],
   "metadata": {}
  },
  {
   "cell_type": "code",
   "source": [
    "# Run this cell if you wish to resume training from a checkpoint\n",
    "sample_data_list = [testing[i] for i in sample(range(len(testing)), 1)]\n",
    "\n",
    "_, sample_target, _ = collate_pool(sample_data_list)\n",
    "normalizer = Normalizer(sample_target)\n",
    "\n",
    "def resumeCheckpoint(checkpoint_path,model,normalizer):\n",
    "  # checkpoint_path: file path of the checkpoint\n",
    "  # model: Model object defined from the code\n",
    "  # normalizer object is already defined\n",
    "  if os.path.isfile(checkpoint_path):\n",
    "            print(\"=> loading checkpoint '{}'\".format(checkpoint_path))\n",
    "            checkpoint = torch.load(checkpoint_path)\n",
    "            start_epoch = checkpoint['epoch']\n",
    "            best_accuracy = checkpoint['best_accuracy']\n",
    "            model.load_state_dict(checkpoint['state_dict'])\n",
    "            optimizer.load_state_dict(checkpoint['optimizer'])\n",
    "            normalizer.load_state_dict(checkpoint['normalizer'])\n",
    "            print(\"=> loaded checkpoint '{}' (epoch {})\"\n",
    "                  .format(checkpoint_path, checkpoint['epoch']))\n",
    "  else:\n",
    "      print(\"=> no checkpoint found at '{}'\".format(checkpoint_path))"
   ],
   "metadata": {},
   "execution_count": null,
   "outputs": []
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "def train_validate_test_model(start_epoch,epochs,**kwargs):\n",
    "\n",
    "  # Train, validate and test the model\n",
    "\n",
    "  best_accuracy = 0\n",
    "\n",
    "  if kwargs['resumeCheckpoint_path']:\n",
    "    resumeCheckpoint_path = kwargs['resumeCheckpoint_path']\n",
    "    resumeCheckpoint(resumeCheckpoint_path,TheModel_1,normalizer)\n",
    "\n",
    "  for epoch in range(start_epoch, epochs):\n",
    "          # train for one epoch\n",
    "          train(train_loader, TheModel_1,optimizer, epoch)\n",
    "\n",
    "          # evaluate on validation set\n",
    "          accuracy = validate(val_loader, TheModel_1, criterion, test=False)\n",
    "\n",
    "          if accuracy != accuracy:\n",
    "            print('Exit due to NaN')\n",
    "            sys.exit(1)\n",
    "\n",
    "          scheduler.step()\n",
    "\n",
    "          # remember the best accuracy and save checkpoint\n",
    "          is_best = accuracy > best_accuracy\n",
    "          is_best = True\n",
    "          print(is_best)\n",
    "          best_accuracy = max(accuracy, best_accuracy)\n",
    "\n",
    "          save_checkpoint({\n",
    "          'epoch': epoch + 1,\n",
    "          'state_dict': TheModel_1.state_dict(),\n",
    "          'best_accuracy': best_accuracy,\n",
    "          'optimizer': optimizer.state_dict(),\n",
    "          'normalizer': normalizer.state_dict()}, is_best)\n",
    "\n",
    "  torch.cuda.empty_cache() # clear the GPU memory cache\n",
    "  # test best model\n",
    "  print('\\n\\n---------Evaluate Model on Test Set---------------\\n\\n')\n",
    "  best_checkpoint = torch.load('model_best.pth.tar')\n",
    "  TheModel_1.load_state_dict(best_checkpoint['state_dict'])\n",
    "  validate(test_loader, TheModel_1, criterion, test=True)\n"
   ]
  },
  {
   "cell_type": "code",
   "source": [
    "# Begin your model training testing and validation\n",
    "train_validate_test_model(start_epoch=0,epochs=3,resumeCheckpoint_path=False)\n",
    "\n",
    "# After training the model will produce a test_result.csv file as the output. The second column is the actual label and the third is the predicted label"
   ],
   "metadata": {},
   "execution_count": null,
   "outputs": []
  },
  {
   "cell_type": "markdown",
   "source": [
    "Use the trained model for classifying new structures\n"
   ],
   "metadata": {}
  },
  {
   "cell_type": "code",
   "source": [
    "def predict(test_loader_pred, model,test=True):\n",
    "  # Monitor prediction time\n",
    "  batch_time = AverageMeter()\n",
    "  data_time = AverageMeter()\n",
    "  if test:\n",
    "      test_targets = []\n",
    "      test_preds = []\n",
    "      test_cif_ids = []\n",
    "\n",
    "    # switch to evaluate mode\n",
    "  model.eval()\n",
    "  end = time.time()\n",
    "\n",
    "  for i, (input, target, batch_cif_ids) in enumerate(test_loader_pred):\n",
    "      with torch.no_grad():\n",
    "                input_var = (Variable(input[0]),\n",
    "                             Variable(input[1]),\n",
    "                             input[2],\n",
    "                             input[3])\n",
    "\n",
    "      # Obtain Model Targets\n",
    "      model.getFeatures_Targets(target,test=True)\n",
    "\n",
    "      # compute output\n",
    "      output = model(*input_var)\n",
    "\n",
    "      # Gather the model predictions\n",
    "      if test:\n",
    "            test_pred = torch.exp(output[0].data.cpu())\n",
    "            assert test_pred.shape[1] == classnum\n",
    "            test_pred = torch.argmax(test_pred,axis=1)\n",
    "            test_preds += test_pred[:].tolist()\n",
    "            test_cif_ids += batch_cif_ids\n",
    "\n",
    "      # measure elapsed time\n",
    "      batch_time.update(time.time() - end)\n",
    "      end = time.time()\n",
    "\n",
    "      if test:\n",
    "        with open('Predicted_results.csv', 'w') as f:\n",
    "            writer = csv.writer(f)\n",
    "            for cif_id, pred in zip(test_cif_ids, test_preds):\n",
    "                writer.writerow((cif_id,pred))\n",
    "\n",
    "\n",
    "  print('\\n\\n---------Done! Pridected outcomes are ready---------------')\n",
    "\n",
    "  return None\n",
    "\n",
    "def predict_with_model(trained_model_path,dataset_path,zipped_cif_path,pridict_number,**kwargs):\n",
    "  # trained_model_path: Location of the trained model\n",
    "  # dataset_path: Locarion of the dataset\n",
    "  # zipped_cif_path: Path to zipped cif files\n",
    "  # pridict_number: Number of samples to predict at one batch\n",
    "\n",
    "    '''\n",
    "    kwargs:\n",
    "      load_checkpoint_path= path for checkpoint\n",
    "      modle = model object variable\n",
    "    '''\n",
    "\n",
    "  # First load the model parameters\n",
    "    if os.path.isfile(trained_model_path):\n",
    "        print(\"=> loading model params '{}'\".format(trained_model_path))\n",
    "        model_checkpoint = torch.load(trained_model_path,\n",
    "                                      map_location=lambda storage, loc: storage)\n",
    "        print(\"=> loaded model params '{}'\".format(trained_model_path))\n",
    "    else:\n",
    "          print(\"=> no model params found at '{}'\".format(trained_model_path))\n",
    "          # optionally resume from a checkpoint (or the best checkpoint)\n",
    "          if os.path.isfile(kwargs['load_checkpoint_path']):\n",
    "                print(\"=> loading model '{}'\".format(kwargs['load_checkpoint_path']))\n",
    "                checkpoint = torch.load(kwargs['load_checkpoint_path'],\n",
    "                                        map_location=lambda storage, loc: storage)\n",
    "                kwargs['model'].load_state_dict(checkpoint['state_dict'])\n",
    "\n",
    "                dataset = CIFData(dataset_path,zipped_cif_path)\n",
    "\n",
    "                sample_data_list = [dataset[i] for i in sample(range(len(testing)), 1)]\n",
    "\n",
    "                _, sample_target, _ = collate_pool(sample_data_list)\n",
    "                normalizer = Normalizer(sample_target)\n",
    "                normalizer.load_state_dict(checkpoint['normalizer'])\n",
    "                print(\"=> loaded model '{}' (epoch {}, validation {})\"\n",
    "                      .format(kwargs['load_checkpoint_path'], checkpoint['epoch'],\n",
    "                              checkpoint['best_accuracy']))\n",
    "          else:\n",
    "                print(\"=> no model found at '{}'\".format(kwargs['load_checkpoint_path']))\n",
    "\n",
    "    if os.path.isfile(trained_model_path) or os.path.isfile(kwargs['load_checkpoint_path']):\n",
    "      if dataset_path:\n",
    "        # load data\n",
    "        dataset = CIFData(dataset_path,zipped_cif_path)\n",
    "        collate_fn = collate_pool\n",
    "        test_loader_pred = DataLoader(dataset, batch_size=pridict_number, shuffle=True,\n",
    "                                  num_workers=1, collate_fn=collate_fn,\n",
    "                                  pin_memory=True)\n",
    "\n",
    "        # build model if model is specified\n",
    "        if os.path.isfile(trained_model_path):\n",
    "            structures, _, _ = dataset[0]\n",
    "            orig_atom_fea_len = structures[0].shape[-1]\n",
    "            nbr_fea_len = structures[1].shape[-1]\n",
    "            model = CrystalGraphConvNet_1(orig_atom_fea_len, nbr_fea_len,\n",
    "                                      classification=True)\n",
    "            model.load_state_dict(model_checkpoint['state_dict'])\n",
    "            predict(test_loader_pred, model,test=True)\n",
    "\n",
    "        else:\n",
    "          predict(test_loader_pred, kwargs['model'],test=True)\n",
    "\n",
    "\n",
    "\n",
    "\n",
    "      else:\n",
    "        print('No dataset was specified, please specify the dataset path')\n",
    "\n",
    "    else:\n",
    "      print('No model was provided, please specify a model or checpoint path')"
   ],
   "metadata": {},
   "execution_count": null,
   "outputs": []
  },
  {
   "cell_type": "code",
   "source": [
    "# Run this block after initiating the CrystalGraphConvNet_1 class at least once from the previous codes above\n",
    "\n",
    "# Note: Make sure that the number of features you are adding to for the predicion are the same as those that the model was trained on.\n",
    "# The model can still recognize any number of additional features besides those that it was trained on. If additional features besides\n",
    "# those that were used to train the model were added, incorrect results would be produced due to noise introduced by the added features\n",
    "# the model was not trained on.\n",
    "\n",
    "#  The code below is an example of how to load the weights of trained model\n",
    "predict_with_model('model_best.pth.tar','hMOF Database','hMOF 10 1039 C2EE23201D all',pridict_number= 30)\n",
    "\n",
    "# The code below is an example of how to load the model from a checkpoint\n",
    "#predict_with_model(trained_model_path = False, dataset_path ='hMOF Database',zipped_cif_path ='hMOF 10 1039 C2EE23201D all',\n",
    "#                  pridict_number=300,model=TheModel_1,\n",
    "#                  load_checkpoint_path = 'checkpoint.pth.tar')\n",
    "\n",
    "# The model will output a Predicted_results.csv file as the predicted class labels"
   ],
   "metadata": {},
   "execution_count": null,
   "outputs": []
  }
 ],
 "metadata": {
  "kernelspec": {
   "display_name": "Python 3",
   "language": "python",
   "name": "python3"
  }
 },
 "nbformat": 4,
 "nbformat_minor": 0
}
